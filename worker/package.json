{
  "name": "ono-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker backend for ONO game",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --env=production",
    "deploy:dev": "wrangler deploy",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241030.0",
    "typescript": "^5.3.3",
    "wrangler": "^4.63.0"
  }
}
